if (!global._babelPolyfill) { require('babel-polyfill'); }

import { assert } from 'chai';

describe.skip('soft delete', () => {
  it('needs tests', () => {
    assert.ok(false);
  });
});

